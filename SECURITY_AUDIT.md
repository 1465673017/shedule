# 项目安全与可靠性审计修复清单

审计日期：2026-07-19

## 修复目标

在不改变现有课表、考勤、统计和工资计算口径的前提下，处理本次全项目审计发现的明显安全漏洞、数据丢失风险和测试缺口。

## 高风险问题

- [x] 动态 HTML 注入：主要学生、科目、年级、考勤及统计渲染入口已转义，导入生成的内部 ID 已净化。
- [x] Electron 外部窗口与导航策略：外部窗口默认拒绝，HTTPS 链接交给系统浏览器，主窗口禁止离开本地入口。
- [x] 覆盖导入事务回滚：覆盖前保存课程、学生、科目和 ERP 快照，失败后完整恢复。
- [x] Electron 与构建依赖：升级至 Electron 43.1.1 和 electron-builder 26.15.3，`npm audit` 为 0。

## 中风险问题

- [x] `timetableData` 损坏恢复：保存最近一次有效数据，主数据解析失败时自动恢复备份。
- [x] `localStorage` 保存失败回滚：保存失败时恢复最后成功保存的核心运行时快照。
- [x] 导入日期严格校验：不存在的年月日不再自动滚动到下个月。
- [x] 文件保存 IPC：限制字符串数据、编码、扩展名、文件名和 50 MB 大小上限。
- [x] Word/Excel 导出：用户文本统一转义，电子表格公式前缀按文本处理。
- [x] 基线内容安全策略：禁止联网、对象、框架、表单提交和外部基础地址。
- [x] 严格脚本策略：迁移所有可执行内联事件，`script-src` 已移除 `unsafe-inline`。

## 测试缺口

- [x] 将时间设置测试加入默认 `npm test`。
- [x] 增加安全策略、导入日期和回滚结构回归测试。
- [x] 增加主要动态文本转义回归检查。
- [x] 增加课时费规则及统计缓存失效测试。

## 验收要求

- 所有用户或导入数据在进入 HTML 上下文前均经过安全转义，或改用 `textContent`。
- 应用窗口只允许加载本地应用页面，外部 HTTPS 链接交给系统浏览器。
- 覆盖导入失败后，课程、学生、科目、考勤和实际时长数据完整恢复。
- 本地数据损坏时应用仍可启动，并优先恢复最近一次有效备份。
- 文件保存 IPC 拒绝异常类型、超大数据、非法编码和非法扩展名。
- `npm test` 覆盖现有测试与新增关键回归测试并全部通过。

## 修复记录

### 2026-07-19

- 修改 `main.js`、数据导入、核心存储、课程/学生/考勤/统计渲染及文件导出逻辑。
- 新增 `tests/security-regressions.test.js` 和 `tests/salary-stats.test.js`。
- 新增 `tests/electron-smoke.test.js`，真实启动 Electron 验证 CSP 与声明式事件绑定。
- 默认测试现包含 ERP、时间设置、工资/缓存、安全回归和全部脚本语法检查。
- 验证命令：`npm test`、`npm audit --json`、`git diff --check`。
- 当前依赖漏洞：0。
- 71 个静态事件已改为不可执行的 `data-on*` 声明，并由白名单解析器安全绑定；动态模板不再生成内联事件。
- 追加完成 Word/Excel 导出转义、公式注入防护、非法文件名净化及保存失败运行时回滚。
