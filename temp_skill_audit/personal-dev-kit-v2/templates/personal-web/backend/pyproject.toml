[project]
name = "personal-web-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.116", "uvicorn>=0.35", "pydantic>=2.11"]

[dependency-groups]
dev = ["pytest>=8", "httpx>=0.28", "ruff>=0.12", "basedpyright>=1.29"]
