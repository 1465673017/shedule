# Data Tool Agent Rules

- Validate every imported file.
- Keep import, transform, validation, and export separate.
- Use Pandas for compatibility and Polars for larger workloads.
- Make date, decimal, timezone, and null handling explicit.
- Export CSV/XLSX/ZIP safely.
- Provide row counts, rejected-row counts, and readable errors.
