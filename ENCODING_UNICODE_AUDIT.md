# 项目编码错误与 Unicode 转义整理

## 说明

本整理只统计项目自有源码中的相关内容，扫描范围为：

- `js/*.js`
- `index.html`
- `main.js`
- `preload.js`
- `styles.css`

未纳入：

- `node_modules`
- `.git`
- 其他第三方/生成文件

这里把问题分成两类：

1. **编码错误 / 乱码**
   常见表现是 `璇剧▼`、`宸叉帓`、`缂栬緫` 这类明显不是正常中文的字符串。
2. **Unicode 转义**
   形如 `\u4e34\u65f6\u6362\u8bfe`、`\ufeff` 的写法，本身不一定是错误，很多时候是有意为之。

---

## 一、编码错误（乱码）位置

### 1. `js/app-courses.js`

这是项目里编码问题最集中的文件，既有注释乱码，也有真实会显示到界面的文案乱码。

#### 注释乱码

- `js/app-courses.js:316`
- `js/app-courses.js:368`
- `js/app-courses.js:506`
- `js/app-courses.js:558`
- `js/app-courses.js:618`
- `js/app-courses.js:628`
- `js/app-courses.js:719`
- `js/app-courses.js:894`
- `js/app-courses.js:920`
- `js/app-courses.js:938`

这些属于“可读性问题”，不会直接影响运行，但说明该文件曾发生过错误转码或以错误编码保存。

#### 会影响界面或提示的乱码文案

- `js/app-courses.js:398`
  `assigned-badge` 的文案是乱码，属于你最初遇到的“试听已排课学生加课时显示乱码”的直接原因之一。

- `js/app-courses.js:652`
- `js/app-courses.js:854`
- `js/app-courses.js:978`
  这几处都是“请选择科目”类提示，但当前源码里是乱码。

- `js/app-courses.js:930`
  “课程已存在，无需重复添加”类提示乱码。

- `js/app-courses.js:948`
  “课程已存在于课表中，无需重复添加”类提示乱码。

- `js/app-courses.js:1014`
  “试听学生不能加入课程池”类提示乱码。

- `js/app-courses.js:1222`
  课程池空状态文案乱码。

- `js/app-courses.js:1264`
  “未排课”文案乱码。

- `js/app-courses.js:1266`
  “已排 X 次”文案乱码。

- `js/app-courses.js:1286`
  编辑按钮 `title` 乱码。

- `js/app-courses.js:1288`
  删除按钮 `title` 乱码。

#### 结论

`app-courses.js` 是当前项目中**唯一明显同时存在“注释乱码 + UI 文案乱码”**的核心文件，优先级最高。

### 2. `js/app-export.js`

- `js/app-export.js:576`

这里只有注释乱码：

- `// 鍒涘缓鍏煎Excel鐨凥TML鏍煎紡`

这类问题不会直接影响功能，但说明该文件也经历过不一致编码处理。

---

## 二、Unicode 转义位置

下面这些不是“编码错误”，而是源码中显式使用了 Unicode 转义。

### 1. `js/app-courses.js`

- `25`
- `47`
- `144`
- `178`
- `218`
- `396`

对应内容主要是：

- “临时换课”
- “临时换课中”
- “编辑课程”
- “添加课程”
- “试”

### 2. `js/app-export.js`

- `273`
- `360`
- `563`
- `695`

这里的 `\ufeff` 不是普通文案，而是 **UTF-8 BOM**：

- `'\ufeff' + wordContent`
- `'\ufeff' + excelContent`

### 3. `js/app-stats.js`

`app-stats.js` 中 Unicode 转义非常集中，主要是统计页的中文文案。检测到的位置包括：

- `1257`
- `1269`
- `1281`
- `1293`
- `1327`
- `1345`
- `1371-1377`
- `1382`
- `1407`
- `1458-1470`
- `1812-1817`
- `1832-1852`
- `1939`
- `2018-2028`
- `2036`
- `2068`
- `2135-2150`
- `2177`
- `2201`
- `2222`
- `2243`
- `2263`
- `2267-2281`
- `2465-2470`
- `2497`
- `2548`
- `2561`

这些内容大多是：

- 图表标签
- 空态提示
- 统计标题
- tooltip 文案
- 卡片说明文字

---

## 三、错误原因

### 1. 为什么会出现这些乱码

最常见的原因是：

1. 文件原本是 UTF-8，但被按 GBK/ANSI 打开后又保存。
2. 文件原本是 GBK/ANSI，但被按 UTF-8 打开后又保存。
3. 同一个文件在不同编辑器、终端、脚本之间来回处理，默认编码不一致。
4. 批量替换、脚本写文件、复制粘贴时使用了错误编码。

像 `璇剧▼`、`宸叉帓`、`缂栬緫` 这类字符串，基本都是**“中文被错误解码后再次保存”**的典型表现，也就是常说的 **mojibake（文字乱码）**。

### 2. 为什么 `app-courses.js` 问题特别多

从分布看，`app-courses.js` 不是单个字符串偶发损坏，而是：

- 注释有乱码
- alert 文案有乱码
- HTML 字符串有乱码
- 按钮 title 有乱码

这通常说明它不是“最近改坏一行”，而是这个文件在某次编辑、合并、脚本处理时整体经历过一次错误编码读写。

---

## 四、为什么会用 Unicode 编码（转义）

这里的“Unicode 编码”更准确地说，是 **Unicode 转义写法**，也就是 `\uXXXX`。

### 1. `\uXXXX` 不等于乱码

例如：

- `\u7f16\u8f91\u8bfe\u7a0b` 实际就是 “编辑课程”
- `\u8bd5\u542c` 实际就是 “试听”

这类写法本身是合法且可运行的 JavaScript 字符串表示方式。

### 2. 项目里为什么会有人这么写

常见原因有：

1. **避免再次被错误转码**
   如果团队环境里有人用的编辑器、终端、脚本编码不统一，把中文写成 `\uXXXX` 可以尽量减少“保存后变乱码”的概率。

2. **某些自动化工具/压缩器会产出这种格式**
   一些构建脚本、代码生成器、拷贝脚本会把非 ASCII 字符自动转成 `\uXXXX`。

3. **跨平台兼容考虑**
   在老旧环境、部分嵌入式 WebView、导出文本拼接场景里，开发者有时会主动使用 Unicode 转义，避免源文件本身的编码差异。

4. **导出文件需要 BOM**
   `\ufeff` 这一类用途更明确。这里不是“显示文字”，而是给导出的 Word/Excel 文本加 BOM，帮助某些软件正确识别 UTF-8。

### 3. 为什么 `app-stats.js` 大量使用 Unicode 转义

从分布看，`app-stats.js` 里的 `\uXXXX` 比较系统、连续、成片，且逻辑完整，没有明显半坏半好的现象。这更像是：

- 该文件曾经过某个工具统一转义
- 或开发时有意避免中文直写

因此它更像“风格/策略选择”，**不是编码错误**。

---

## 五、如何判断“乱码”与“Unicode 转义”谁该修

### 应该修复的

- 用户会看到的乱码文案
- 注释中影响维护的乱码
- 造成语义不清的字符串

尤其是：

- `js/app-courses.js:398`
- `js/app-courses.js:652`
- `js/app-courses.js:930`
- `js/app-courses.js:948`
- `js/app-courses.js:1014`
- `js/app-courses.js:1222`
- `js/app-courses.js:1264`
- `js/app-courses.js:1266`
- `js/app-courses.js:1286`
- `js/app-courses.js:1288`

### 不一定要修的

- `\uXXXX` 形式的合法字符串
- `\ufeff` 这种导出兼容用途

如果团队更重视可读性，可以把这些 `\uXXXX` 统一改回中文直写；如果团队更重视“避免再出乱码”，也可以保留。

---

## 六、建议

### 1. 优先处理顺序

1. 先修 `js/app-courses.js` 中所有用户可见乱码。
2. 再修 `js/app-courses.js` 和 `js/app-export.js` 的乱码注释。
3. 最后再决定是否把 `app-stats.js` 中的 `\uXXXX` 批量改为中文直写。

### 2. 编码规范建议

- 所有源码统一保存为 `UTF-8`
- 编辑器统一设置 `UTF-8`
- Git 相关工具链避免自动按本地 ANSI/GBK 改写文件
- 若继续保留 `\uXXXX`，建议只在“容易被转码破坏的文件”或“导出兼容场景”使用
- 若改回中文直写，建议一次性统一转换并做编码校验

### 3. 对当前项目的判断

- **编码错误的核心问题**：`js/app-courses.js`
- **少量注释错误**：`js/app-export.js`
- **Unicode 转义的主要集中区**：`js/app-stats.js`
- **BOM 用法属于合理用途**：`js/app-export.js`

---

## 七、一句话结论

这个项目里“真正的编码错误”主要集中在 `js/app-courses.js`，而“Unicode 编码的地方”主要集中在 `js/app-stats.js`，后者多数不是 bug，而更像是为了规避编码风险或由工具生成的转义写法。
