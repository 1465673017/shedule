# 性能优化待办

更新日期：2026-07-29

学生池卡顿已单独修复。以下是代码审计中发现、尚未处理的性能问题。

## 高优先级

### 1. `getCellVersion()` 隐式重建完整课表投影

- 位置：`js/app-erp.js`
- 现状：每次查询单个课位版本前都会执行 `buildTimetableProjection(app)`。
- 影响：任何循环内的课位查询都会退化成多次全量重建。
- 建议：引入投影失效标记；仅在数据变化后重建一次，普通查询只读取缓存。

### 2. 周课表渲染逐格触发全量重建

- 位置：`js/app-timetable.js` 的 `renderTimetable()`
- 现状：每个日期、节次单元格分别调用 `getCellVersion()`。
- 影响：切换周、刷新课表、修改课程后可能出现明显卡顿。
- 建议：渲染开始前统一构建一次投影，循环中使用只读查询。

### 3. 统计与课时导出重复查询版本

- 位置：`js/app-stats.js`、`js/app-export.js`
- 现状：按日期和节次循环调用 `getCellVersion()`；部分统计流程还会通过 `isClassFinished()` 再查一次。
- 影响：日期范围越长，统计和导出耗时增长越明显。
- 建议：一次构建投影，并按日期、课位批量读取；复用已取得的版本数据。

### 4. `syncRealtime()` 重复执行重计算和重绘

- 位置：`js/app-core.js`
- 现状：一次同步会执行结课检查、投影构建、保存、课表重绘和学生池重绘；下游渲染还可能再次构建投影。
- 影响：常规编辑操作会产生多轮重复工作。
- 建议：合并同一事务内的构建与渲染，按实际变化选择性刷新。

## 中优先级

### 5. 课表投影构建采用嵌套过滤

- 位置：`js/app-erp.js` 的 `buildTimetableProjection()`
- 现状：遍历每个课程实例时，都会重新过滤完整的学生课程关系列表。
- 影响：课程实例和关系数据同时增长时，构建成本接近平方级。
- 建议：预先建立 `courseInstanceId -> relations` 索引。

### 6. 自动结课检查存在多层全表扫描

- 位置：`js/app-erp.js` 的 `completeStudentsForEndedStages()`
- 现状：阶段、学生、课程实例和课程关系之间存在嵌套遍历。
- 影响：每次 `syncRealtime()` 都可能触发，历史数据多时编辑操作变慢。
- 建议：建立学生到课程、实例到关系的索引，并仅在相关数据变化时重新计算。

### 7. 范围同步、检查和批量复制循环查询课位

- 位置：`js/app-course-import.js`、`js/app-core.js`、`js/app-timetable.js`
- 现状：日期范围或目标课位循环中重复调用 `getCellVersion()`。
- 影响：长范围检查、同步或批量复制时可能阻塞界面。
- 建议：操作开始时构建一次快照，批处理中使用只读查询。

## 验收建议

- 使用包含大量历史课程和学生关系的实际数据测试。
- 分别记录学生池切换、周切换、统计打开、课时导出和范围同步耗时。
- 确认优化前后课程、循环规则、临时学生、暂停及结课状态结果一致。
