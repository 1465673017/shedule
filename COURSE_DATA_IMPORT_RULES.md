# 课程数据导入实现细则

## 1. 可识别的数据

导入器接受单条课程对象、课程对象数组和完整接口响应。解析器会递归扫描任意对象键，因此支持 `data -> 教师 ID -> 课程数组` 这类教师 ID 动态变化的批量结构，也支持多个教师分组。有效课程至少需要 `courseDate` 和 `students`，并使用 `courseTime`、`courseEndTime` 定位课时。

每名学生提取并保存：姓名（`students[].name`）、年级（`grade.name`）、班型、上课日期与时间、出勤状态。外部学生 ID 会保存为 `externalStudentId`，重复导入时优先用它合并学生；无外部 ID 时按“姓名 + 年级”合并。

## 2. 班型规则

- `type` 为 `ONE_ON_ONE_COURSE`，或课程名称、类型、标签明确包含 `1v1` / `1对1` / `一对一` 时，学生标记为 `1v1`，学生池和课表显示 1v1 标签。
- 其他所有班型（包括 `SMALL_CLASS`、1v2、1v3、1v4 等）统一保存为 `1vN`。
- 同一学生被新导入课程识别出的班型以最新导入结果为准。

## 3. 日期、时间与循环

- 课程按 `courseDate` 的实际星期加入课表，按 `courseTime-courseEndTime` 精确匹配基础设置中的课时；找不到课时则整次导入报错，不猜测位置。
- 导入课程从课程日期所在周开始按周循环。
- 导入会在对应“星期 + 课时”写入来源为 `course-import` 的显式版本。该版本优先于此前的人工排课和循环结果，从导入周起自动截断该位置的旧课表。
- 截断只影响导入周及以后，历史周保持不变。
- 之后的人为拖动、删除、修改会产生更新版本并结束原导入循环；再次导入则由新导入版本继续覆盖。
- 同一批数据中相同位置出现多条课程时，按 JSON 中的顺序处理，后一条生效。

## 4. 科目与出勤

- 科目按 `subject.name` 复用；不存在时自动加入科目池。
- 出勤明细优先读取 `attendentDetail` 中对应学生的状态，也兼容学生对象上的 `attendentStatus` / `attendanceStatus`。
- 已识别状态映射为：出勤 `present`、请假 `leave`、缺勤 `absent`。
- 示例中的 `attendentDetail: null` 且课程为 `NEW`，只能确定三人“待考勤”，因此不伪造个人出勤记录；界面按未考勤处理。

## 5. 导入结果与错误

点击左侧“导入课程数据”，粘贴 JSON 后执行“识别并导入”。成功后保存学生池、科目池、课表版本和可识别的考勤记录。JSON 非法、无有效课程、日期非法或课时无法匹配时显示错误；错误发生前已经处理的多课程可能已经写入，因此建议一次导入同一来源、同一周的数据。
