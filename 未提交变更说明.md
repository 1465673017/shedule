# 未提交变更说明

生成日期：2026-07-27

## 1. 变更概况

当前工作区相对最近一次 Git 提交共有：

- 13 个已修改文件；
- 约 2078 行新增、130 行删除；
- 1 个未跟踪的 Python 缓存目录 `__pycache__/`。

已修改文件：

- `index.html`
- `styles.css`
- `main.py`
- `js/app-core.js`
- `js/app-course-import.js`
- `js/app-erp.js`
- `js/app-attendance.js`
- `js/app-courses.js`
- `js/app-timetable.js`
- `js/app-stats.js`
- `js/app-export.js`
- `tests/schedule-erp.test.js`
- `tests/security-regressions.test.js`

未跟踪生成物：

- `__pycache__/main.cpython-38.pyc`

`__pycache__` 是 Python 运行产生的字节码缓存，不属于业务源码，提交前建议删除或加入 `.gitignore`。

## 2. 课表数据检查与选择性同步

涉及文件：

- `index.html`
- `styles.css`
- `js/app-core.js`
- `js/app-course-import.js`
- `main.py`

主要变化：

1. 新增“检查”入口和“检查课表数据”弹窗。
2. 支持两种检查模式：
   - 时段抽检，当前默认模式；
   - 定点抽检。
3. 新增日期范围日历，支持选择起止日期及切换月份。
4. 检查过程只读取网络课程并与本地课表比较，不会直接修改本地数据。
5. 检查内容包括：
   - 课程日期和课位；
   - 科目；
   - 上课时间；
   - 学生名单；
   - 考勤状态；
   - 学生实际上课时长。
6. 检查区只保留最新一次检查数据。开始新检查时会清除旧结果并作废旧网络快照。
7. 每一行差异可以选择：
   - 本地数据：同步时保留该课位；
   - 网络数据：同步时用最新网络快照替换该课位。
8. 选择状态取消文字标签，改为整张卡片使用主题色边框和底色高亮。
9. 必须为全部差异完成选择后才允许执行“一键同步”。
10. 一键同步只处理最新一次完整检查快照，不使用历史检查数据。
11. 同步网络选项时只替换对应课位；选择本地的课位不会被修改。
12. 网络侧不存在课程且选择网络时，会删除对应的本地单次课程。
13. 同步采用事务式快照保护。发生异常时恢复同步前的科目、学生和 ERP 课表数据。
14. 同步完成后立即重建课表投影、刷新当前课表、科目区域和周范围，不需要刷新页面。
15. 检查弹窗界面调整：
   - “一键同步”移动到“开始检查”旁边；
   - 移除底部“关闭”按钮和“清除”按钮；
   - 仍可使用右上角关闭按钮；
   - 标题和副标题左对齐；
   - 缩短时段日期选择框宽度，使开始检查按钮更靠近日期控件。
16. Python 同步桥新增检查专用事件：
   - `check-basic`
   - `check-progress`
   - `check-attendance`
   - `check-done`

## 3. 普通同步、当天同步与自动同步

涉及文件：

- `index.html`
- `styles.css`
- `js/app-core.js`
- `js/app-course-import.js`
- `main.py`

主要变化：

1. 登录同步弹窗的日期输入改为统一的弹出式日期范围选择器。
2. 新增“同步当天”按钮。
3. 同步当天只更新当天已有课程的考勤和实际上课时长。
4. 自动同步规则改为：
   - 每 2 小时检查一次；
   - 只同步当天课程考勤；
   - 登录成功且当前没有其他同步任务时才执行。
5. 移除原来的“次日异步同步”和“指定时间同步”配置。
6. 手动同步前检测所选范围内是否存在本地手动课程、手动考勤或人员调整。
7. 检测到手动数据时允许用户选择：
   - 完全使用线上数据覆盖；
   - 保留手动修改并合并同步。
8. `attendanceOnly` 模式不会重新导入基础课程，只更新已有课程的考勤数据。
9. 同步导入支持单次课程写入，避免网络课程意外形成循环排课。
10. 网络导入时保存课程的真实开始时间、结束时间、标准课时时间及非标准时间标记。

## 4. 网络课程导入规则

涉及文件：

- `js/app-course-import.js`
- `js/app-erp.js`

主要变化：

1. 网络课程根据开始时间匹配一个起始课位，不再按与多个标准课时的重叠区间拆成多节。
2. 学生实际上课时长直接使用网络返回的实际分钟数或实际起止时间，不再按标准课位重叠比例分摊。
3. 支持以下导入选项：
   - `onlyExisting`：只更新本地已存在的课程；
   - `singleOccurrence`：只建立当前周的单次课程；
   - `preserveManual`：保留手动课程名单、考勤和手动实际时长。
4. 覆盖导入前建立快照，导入失败自动恢复。
5. 课程实例新增或保留以下时间字段：
   - `actualStartTime`
   - `actualEndTime`
   - `standardStartTime`
   - `standardEndTime`
   - `isNonStandardTime`
   - `timeSource`
   - `timeManuallyAdjusted`
   - `importSourceTime`
   - `importTotalMinutes`
6. 单次课程写入可选择不恢复继承得到的下一周版本，避免导入影响后续周次。

## 5. 非标准上课时间与考勤编辑

涉及文件：

- `js/app-attendance.js`
- `js/app-courses.js`
- `js/app-timetable.js`
- `js/app-erp.js`
- `styles.css`

主要变化：

1. 课程新增/编辑弹窗增加实际上课起止时间编辑能力。
2. 开始时间必须落在该标准课时范围内。
3. 结束时间可以在标准结束时间后继续延长。
4. 保存前检查同一天课程时间冲突，冲突时阻止保存并提示重叠课程。
5. 考勤弹窗的课程时间改为可点击按钮，可通过滑块按 5 分钟调整开始和结束时间。
6. 调整课程时间后同步更新：
   - 课程实际起止时间；
   - 课程实际总分钟数；
   - 未被单独手动设置的学生实际时长；
   - 课表显示。
7. 学生实际时长滑块上限不再固定为 240 分钟，会根据当前课程时长动态扩大。
8. 鼠标滚轮调整时长的方向和边界处理被修正。
9. 请假、缺勤或零分钟统一显示为 `0h`，不再显示 `0min`。
10. 移动课程到其他标准课位后，会把课程时间重置为目标课位的标准时间，避免保留原课位时间。
11. 非标准时间课程在课表和统计中增加视觉提示及悬浮说明。
12. 判断课程是否结束时优先采用课程实例的实际结束时间，而不是固定标准课时结束时间。

## 6. 课程明细与统计时长

涉及文件：

- `js/app-stats.js`

主要变化：

1. 构建课程统计数据时读取每名学生的实际上课时长。
2. 课程明细标题不再固定采用计划时长或课程级 `2h`。
3. 每节课程的实际时长按实际出勤学生中的最长实际时长计算。
4. 多节课程合并显示时，累加每一节课程的实际时长。
5. 全员请假或全员缺勤时，该节实际时长为 `0h`。
6. 课程标题、悬浮提示和学生明细现在使用一致的实际时长来源。
7. 分班型统计按学生实际在线时段拆分：
   - 同时 4 人上课的部分计入 `1v4`；
   - 剩余 3 人上课的部分计入 `1v3`；
   - 依次类推。

示例：

- A、B 上课 90 分钟；
- C 上课 120 分钟；
- D 上课 40 分钟。

拆分结果：

- `1v4`：40 分钟；
- `1v3`：50 分钟；
- `1v1` 或原有单人分类：30 分钟。

## 7. 课时单 Excel 导出

涉及文件：

- `js/app-export.js`

### 7.1 工作表“课时统计汇总”

1. 汇总不再在学生时长一致时回退到课程固定计划时长，修复大量课程统一显示 `2h` 的问题。
2. 所有正常数据均按每名出勤学生的实际上课分钟数拆分班型。
3. 同一节课学生时长不同，会按仍在上课的人数分段累计到 `1对1`、`1对2`、`1对3`、`1对4`。
4. 汇总只把分钟换算为小时，不乘年级系数或班型系数。
5. 仅当旧数据完全没有学生实际时长字段时，才兼容性回退到课程时长。

### 7.2 工作表“课时统计1”（Excel 中的工作表2）

1. 实际时长优先取学生实际分钟数，不再优先使用课程级的 `2h`。
2. 多名出勤学生时，使用最长学生实际时长作为当节课程实际时长。
3. 学生实际 80 分钟时导出为 `1h20min`。
4. 全员请假或全员缺勤时：
   - 类型显示 `-`；
   - 实际时长显示 `0h`。
5. 单独的 `-` 不再被电子表格公式防护逻辑加上可见撇号。
6. 颜色规则只依据当节实际时长：
   - `0h`：红色；
   - 大于 `0h` 且不足 `2h`：黄色；
   - 正好 `2h`：无填充；
   - 超过 `2h`：红色。
7. 移除“存在请假学生”或“学生时长不一致”自动触发黄色的规则。

### 7.3 工作表“课时统计2”（Excel 中的工作表3）

1. 每名学生单独使用自己的实际上课时长。
2. 请假或缺勤学生时长显示 `0h`。
3. 请假学生的类型显示 `-`。
4. 颜色规则与工作表2一致：
   - `0h`：红色；
   - 大于 `0h` 且不足 `2h`：黄色；
   - 正好 `2h`：无填充；
   - 超过 `2h`：红色。

## 8. 界面样式

涉及文件：

- `styles.css`

新增或调整的主要样式：

1. 课程检查弹窗布局、结果列表、左右差异卡片和选中高亮。
2. 检查日期与普通同步日期的弹出日历。
3. 登录同步工具栏和按钮紧凑布局。
4. 非标准课程时间标记和悬浮提示。
5. 课程编辑弹窗的小时/分钟选择器。
6. 考勤弹窗的课程起止时间滑块编辑器。
7. 深色主题下新增控件的配色和悬浮状态。

## 9. 测试变更

涉及文件：

- `tests/schedule-erp.test.js`
- `tests/security-regressions.test.js`

新增或更新的覆盖点：

1. 学生不同时长的分班型拆分。
2. Excel 汇总使用原始小时数，不应用系数。
3. 课程实际时长必须按具体日期读取，不能串用其他周次。
4. 工作表3区分零时长、不足两小时和超过两小时。
5. 网络导入日期、考勤状态、实际分钟数和课位匹配。
6. 导入失败快照恢复入口。
7. 工作表中的 `-` 保持为纯文本。
8. 请假学生导出类型为 `-`、时长为 `0h`。
9. 课程级时长为 120 分钟但学生实际为 80 分钟时，导出必须为 `1h20min`。
10. 检查动态 HTML 不使用内联事件处理器，继续满足 CSP 安全限制。

## 10. 当前验证结果

已执行：

```text
npm test
git diff --check
```

结果：

- Schedule ERP tests passed
- Time settings tests passed
- Salary and stats cache tests passed
- Week stats tests passed
- Security regression tests passed
- 所有 JavaScript 语法检查通过
- `git diff --check` 未发现空白字符错误

## 11. 提交前建议

1. 删除或忽略 `__pycache__/`，不要把 Python 字节码缓存提交到仓库。
2. 手动验证一次完整流程：
   - 登录；
   - 时段抽检；
   - 每行选择本地或网络；
   - 一键同步；
   - 确认课表实时刷新。
3. 使用包含以下数据的样例导出课时单并在 Excel 中检查：
   - 学生实际 80 分钟；
   - 全员请假；
   - 全员缺勤；
   - 实际 `0h`；
   - 不足 `2h`；
   - 正好 `2h`；
   - 超过 `2h`；
   - 同一课程学生实际时长不同。
