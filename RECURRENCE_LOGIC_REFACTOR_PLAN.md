# 循环课程逻辑重构实施细则

## 1. 目标

本次重构不改变课程导入、永久删除、日期范围清理、临时课程和考勤的业务含义，只统一循环课程的版本切分机制。

需要解决的问题：

1. 单周删除只删除选中周，前一周显示“中止”，下一周作为新循环起点。
2. 连续删除形成的空白区间，可以由区间首周新增的课程自动填充，直到下一节真实课程前停止。
3. 双击、回收区、移动、临时课程等入口不再各自复制循环切分代码。
4. 删除版本时同步清理无主学生关系、重复规则和异常规则。
5. 一个复合操作结束后只进行必要的课表投影重建。
6. 移除课表单元格的双击删除行为，避免误操作；删除必须使用明确的删除入口或拖入回收区。

## 2. 必须保持的兼容规则

- 单周删除：下一周恢复原循环。
- 连续单周删除：每个选中周保持空白，最后一个删除周之后恢复。
- 新增课程：清除下一节真实课程之前的连续空白截止版本，不覆盖下一节真实课程。
- 临时课程：只影响当前周，下一周恢复原课程。
- 移动课程或学生：当前周产生分支，下一周恢复源位置和目标位置原状态。
- 起止日期清理：只清理范围内课程；有结束日期时在范围后恢复，无结束日期时永久截止。
- 课程池删除、科目删除、学生删除：仍是全局业务操作，不转换成单周删除。
- 课程导入：仍从导入周开始显式覆盖，不自动桥接导入后的空白截止版本。
- 学生的循环、临时、中止、结课状态必须在恢复分支中继承。
- 考勤记录和实际课时必须继续绑定正确的课程实例。

## 3. 实施步骤

### 阶段一：统一空白截止与关联数据清理

1. 用一个带范围策略的内部清理函数替代两个重复函数。
2. `until-next-course` 模式只清理连续空白截止，遇到真实课程立即停止。
3. `all-future-empty` 模式用于重新开启循环，清理后续空白截止，但不删除真实课程。
4. 清理课程实例时同步清理：
   - `studentCourseRelations`
   - `repeatRules`
   - `exceptionRules`
5. 不清理考勤历史，除非调用方明确执行删除考勤业务。

验收：连续删除 7.9、7.16、7.23 后，在 7.9 新增课程能够循环到 7.23，并在 7.30 的真实课程前中止。

### 阶段二：统一单周切分与恢复

1. 保留一个公共服务方法负责：
   - 读取当前周和下一周快照；
   - 写入当前周截止版本；
   - 按需要恢复下一周；
   - 继承学生分支状态和实际课时；
   - 清理被替换实例的关联数据。
2. 单周整课删除调用该方法。
3. 临时课程、移动学生、移动整课逐步复用同一分支恢复辅助方法。
4. 日期范围清理继续保留范围语义，仅复用“恢复快照”能力。

验收：删除、移动、临时课程及范围清理原有测试全部通过。

### 阶段三：统一删除入口并移除双击删除

1. 删除 `timetable` 上的 `dblclick` 删除事件监听器。
2. 拖入左侧回收区统一调用单周删除服务。
3. 显式删除按钮或菜单继续调用 `removeItemFromCell`。
4. 删除兼容性回退分支；页面固定先加载 `app-erp.js`，服务缺失应直接暴露开发错误。

验收：双击课程不再删除；回收区和显式删除只删除当前周。

### 阶段四：清理无效数据

1. 空截止版本不创建空课程模板。
2. 删除实例不创建没有业务意义的循环规则。
3. 增加统一的孤儿数据修剪函数，只删除找不到课程实例的数据关系。
4. 保留考勤历史的显式业务处理，不在通用修剪中隐式删除。

验收：删除后不存在指向缺失课程实例的学生关系、重复规则和异常规则。

### 阶段五：控制投影重建

1. 服务增加批处理深度/脏标记。
2. 单独调用服务时保持立即投影，兼容现有代码。
3. 复合操作在批处理结束时只重建一次。
4. `syncRealtime` 的界面刷新行为保持不变。

验收：业务测试不变，并验证复合操作不会在中间状态暴露错误投影。

## 4. 测试清单

- 单周删除后下一周恢复。
- 连续删除三周后仅保留首周和后续真实课程。
- 删除区间首周新增课程后自动桥接空白周。
- 桥接课程最后一周显示“中止”。
- 未来真实课程不被覆盖。
- 临时课程下一周恢复。
- 移动学生只影响当前周。
- 移动整课只影响当前周。
- 中止、临时、结课规则在恢复分支继续生效。
- 课程导入仍按导入规则覆盖。
- 双击课程不触发删除。
- 回收区删除只删除当前周。
- 不产生无主学生关系、重复规则和异常规则。

## 5. 回滚边界

每个阶段独立提交逻辑并运行测试。若某阶段失败，只回退该阶段实现，不回退已经验证的数据一致性修复。任何全局删除、导入覆盖或范围清理行为出现变化时，立即停止后续阶段并先修复兼容问题。

## 6. 本次实施记录

- [x] 阶段一：两个空白截止清理函数已合并，并统一清理实例关联图。
- [x] 阶段二：已提供统一的课程快照恢复原语，单周删除、临时课程、移动和范围恢复已接入。
- [x] 阶段三：课表容器和单元格上的双击删除监听均已移除；显式删除和回收区使用统一服务。
- [x] 阶段四：空截止不再创建空模板或循环规则，已增加孤儿数据修剪。
- [x] 阶段五：删除恢复流程末尾的重复投影已移除；需要读取中间版本的投影仍保留，以避免改变移动和范围恢复语义。
