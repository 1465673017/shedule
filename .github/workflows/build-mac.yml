name: Build macOS Packages

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.x'
        cache: 'npm'

    - name: Set up universal Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        architecture: 'x64'
        cache: 'pip'
        cache-dependency-path: build/requirements-mac.txt

    - name: Install macOS bridge dependencies
      run: python3 -m pip install -r build/requirements-mac.txt

    - name: Install dependencies
      run: npm ci

    - name: Run tests
      run: |
        npm test
        python3 -m py_compile main.py

    - name: Cache electron-builder
      uses: actions/cache@v4
      with:
        path: |
          ~/Library/Caches/electron
          ~/Library/Caches/electron-builder
          ~/.cache/electron-builder
        key: ${{ runner.os }}-electron-builder-${{ hashFiles('package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-electron-builder-

    - name: Build macOS packages
      env:
        CSC_IDENTITY_AUTO_DISCOVERY: false
      run: npm run build:mac

    - name: Upload macOS green ZIP
      uses: actions/upload-artifact@v4
      with:
        name: macos-green-zip
        path: dist/*绿色压缩包版*.zip
        if-no-files-found: error

    - name: Upload macOS portable ZIP
      uses: actions/upload-artifact@v4
      with:
        name: macos-portable-zip
        path: dist/*便携版*.zip
        if-no-files-found: error

    - name: Upload macOS DMG
      uses: actions/upload-artifact@v4
      with:
        name: macos-dmg
        path: dist/*.dmg
        if-no-files-found: error
