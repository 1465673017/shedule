# 课表主界面布局改版方案

## 文档目标

这份文档聚焦当前课表主界面的三个核心区域：

- 顶部右上角菜单栏
- 左侧课程池 / 学生池
- 中间课表主体区域

目标不是笼统评价“好不好看”，而是把当前不美观的原因拆解成可执行的布局问题，并给出一套适合后续落地的改版方向。

涉及的主要代码位置：

- 页面结构：[index.html](/E:/VScdoe/class/02/kebiao_extracted/index.html:10)
- 顶部栏样式：[styles.css](/E:/VScdoe/class/02/kebiao_extracted/styles.css:82)
- 主体容器样式：[styles.css](/E:/VScdoe/class/02/kebiao_extracted/styles.css:106)
- 左侧栏样式：[styles.css](/E:/VScdoe/class/02/kebiao_extracted/styles.css:222)
- 课程池样式：[styles.css](/E:/VScdoe/class/02/kebiao_extracted/styles.css:345)
- 课表容器样式：[styles.css](/E:/VScdoe/class/02/kebiao_extracted/styles.css:589)

---

## 一、为什么现在会觉得“不美观”

当前页面的主要问题，不是单个按钮难看，也不是单个颜色选错，而是三个区域没有建立统一的视觉秩序。

### 1. 三个区域像三套不同风格的界面

现状：

- 顶部栏是白底工具条
- 左侧栏是整块蓝色渐变侧边栏
- 课表区是白色卡片内容面板

为什么会不好看：

- 它们分别像“后台工具栏”“侧边导航”“内容卡片”，但没有被统一成一个完整产品界面
- 用户会感觉页面是“拼起来的”，而不是“设计出来的”

对应代码：

- 顶部栏：[styles.css](/E:/VScdoe/class/02/kebiao_extracted/styles.css:82)
- 左侧栏：[styles.css](/E:/VScdoe/class/02/kebiao_extracted/styles.css:222)
- 课表容器：[styles.css](/E:/VScdoe/class/02/kebiao_extracted/styles.css:589)

### 2. 视觉重心不明确

现状：

- 左侧栏颜色最重
- 顶部按钮也都比较抢眼
- 课表本体虽然是主功能，但没有明显成为唯一主角

为什么会不好看：

- 页面里多个区域同时在争夺注意力
- 用户第一眼不知道该先看左边、右上角，还是中间课表

### 3. 间距节奏不统一

现状：

- 顶部栏、主体内容、左侧池子内部都用了不同的内边距和间距
- 主区还有额外的 `padding-left: 40px`

为什么会不好看：

- 对齐线不干净
- 页面会有“松一块、紧一块”的感觉

对应代码：

- 主体容器：[styles.css](/E:/VScdoe/class/02/kebiao_extracted/styles.css:106)
- 池子 tab 和按钮区：[styles.css](/E:/VScdoe/class/02/kebiao_extracted/styles.css:358)
- 筛选区：[styles.css](/E:/VScdoe/class/02/kebiao_extracted/styles.css:384)

### 4. 信息层级不清楚

现状：

- 左侧“课程池 / 学生池”
- “添加课程 / 添加学生”
- 学生筛选按钮
- 课程卡片列表

这些内容几乎被堆在同一层视觉权重里。

为什么会不好看：

- 用户难以快速理解哪一层是导航，哪一层是操作，哪一层是内容
- 不是内容太多，而是缺少层级

### 5. 当前文案乱码会进一步拉低精致感

现状：

- `index.html` 中有较明显乱码文本

为什么会不好看：

- 即使布局不动，乱码也会强烈破坏界面质感
- 用户会下意识把“编码问题”和“设计粗糙”归为同一类印象

对应代码：

- 页面顶部按钮区域：[index.html](/E:/VScdoe/class/02/kebiao_extracted/index.html:10)

---

## 二、三个重点区域的具体问题

## 1. 顶部右上角菜单栏的问题

### 现状

- 顶部栏从左侧栏右边直接开始铺开
- 所有按钮都堆在最右侧
- 按钮类型较多，主次不明显
- 样式上既有 `danger`，又有 `secondary`、`tertiary`

### 为什么会别扭

- 一个工具栏里不应该有太多“同级重要”的按钮
- `查看教程` 使用危险色，会让视觉判断失真
- 菜单栏没有承担“页头”的角色，只像零散操作集合

### 修改建议

- 顶部栏改成轻量页头，而不是纯操作栏
- 最多保留一个强主按钮，其余按钮弱化
- 建议优先级：
  - 主按钮：`导出`
  - 次按钮：`重置`
  - 弱按钮或图标按钮：`教程`、`设置`
- 如果后续允许改结构，可以在顶部左侧补一个标题区，例如“课表编辑器 / 当前周次”

### 样式建议

- 高度统一到 `64px`
- 左右内边距统一到 `24px` 或 `28px`
- 背景改为浅色或轻微半透明，而不是纯白硬切
- 底部分隔线保留，但阴影减弱

### 优先级

高

---

## 2. 左侧课程池 / 学生池的问题

### 现状

- 左侧栏整块蓝色渐变，视觉很重
- 池子内部同时包含 tab、添加按钮、筛选按钮和卡片列表
- 课程卡片本身还有色条、边框、阴影、圆形操作按钮

### 为什么会别扭

- 左侧栏在视觉上比课表主体还抢
- 一个辅助操作区不应该比内容主体更有存在感
- 内部信息密度高，但层级区分不足

### 修改建议

- 左侧栏整体弱化，让它更像“辅助面板”
- 不建议继续使用高饱和整块蓝渐变作为默认风格
- 可以改为两种方向之一：

方向 A：浅色侧栏

- 背景用浅灰蓝或浅暖白
- 保留边界和轻阴影
- 让课表区成为唯一主视觉

方向 B：保留色彩，但降低强度

- 把现在的渐变换成更浅、更低饱和版本
- 降低左侧栏阴影和对比度

### 内部层级建议

- 一级：`课程池 / 学生池` tab
- 二级：`添加课程 / 添加学生`
- 三级：筛选按钮
- 四级：卡片列表

不建议继续把这些内容挤在一个紧凑头部里。

### 卡片建议

- 课程卡片建议简化成“色条 + 名称”为主
- 编辑 / 删除按钮默认弱化，hover 再增强
- 阴影减弱，少一点悬浮卡片感

### 优先级

高

---

## 3. 课表主体区域的问题

### 现状

- 课表容器本身是一个白色卡片
- 表格本体又有自己的一层圆角和阴影
- 日期导航区也是一个独立小卡片

### 为什么会别扭

- 容器套容器，阴影叠阴影，视觉偏厚
- 课表作为主要内容，反而没有做到“简洁、稳定、易扫视”
- 现在更像一组卡片，而不是一个明确的工作台

### 修改建议

- 课表区应该成为页面视觉核心
- 外层容器保留轻卡片感
- 内层表格尽量扁平，不要再强调过多阴影
- 日期导航区与课表标题区统一成一个顶部控制带，避免碎成两块

### 样式建议

- 课表外层圆角可以升级到 `16px`
- 外层保留轻阴影
- 表格内层去掉明显漂浮感
- 表头 hover 效果可以保留，但建议更克制

### 优先级

高

---

## 三、推荐的整体改版方向

如果目标是“更美观，同时不大动功能逻辑”，推荐采用下面这套方向：

## 方向：轻产品化工作台

核心原则：

- 课表是唯一主角
- 左侧池子是辅助面板
- 顶部栏是轻页头，不是强操作区
- 统一圆角、统一间距、统一颜色层级

### 推荐版式

- 左侧固定栏：`240px - 256px`
- 顶部栏高度：`64px`
- 主内容区：大面积留白包住课表
- 课表容器：大卡片，轻阴影，清晰边界

### 推荐视觉关系

- 背景：浅色统一底
- 左侧栏：弱对比
- 顶部栏：弱存在
- 课表：最强存在

---

## 四、可直接执行的改动清单

如果后续准备真正动手改，建议按这个顺序推进：

1. 先统一整体布局骨架

- 调整顶部栏高度和左右边距
- 调整主内容区的对齐线
- 缩减左侧栏宽度或降低其视觉重量

2. 再重做顶部栏层级

- 只保留一个主按钮
- 其余按钮降级
- 必要时补一个标题区

3. 再重做左侧池子头部结构

- tab 独立成第一层
- 添加按钮独立成第二层
- 筛选按钮独立成第三层

4. 再简化课程卡片

- 降低阴影
- 收敛按钮存在感
- 减少视觉噪音

5. 最后收敛课表区样式

- 外层卡片保留
- 内层表格扁平化
- 导航区与表格头部风格统一

---

## 五、建议统一的视觉规范

为了避免“改一点像一点”，建议先建立最基础的界面规范：

### 1. 圆角

- 小控件：`8px`
- 按钮 / tab / 小卡片：`10px - 12px`
- 大容器：`16px`

### 2. 间距

- 小间距：`8px`
- 常规间距：`12px`
- 分组间距：`16px`
- 区块间距：`24px`

### 3. 颜色

- 1 个主色：用于当前选中、关键交互
- 1 个强调色：用于提醒或少量重点
- 中性色：用于边框、背景、正文

不建议让危险色、主色、渐变色同时高频抢注意力。

### 4. 阴影

- 页面最多只保留 1 到 2 个层级的阴影
- 左侧栏、顶部栏、课表、卡片不要全部都很“浮”

---

## 六、这一版最值得优先解决的点

如果只改最有价值的三项，建议优先做：

1. 弱化左侧栏，让课表重新成为视觉中心
2. 调整顶部菜单主次，减少右上角按钮混战
3. 统一三块区域的间距、圆角和颜色逻辑

这三项改完，哪怕功能完全不变，整体观感也会明显从“拼装页面”变成“有秩序的产品界面”。

---

## 七、下一步建议

这份文档适合作为改版基线。后续可以继续往下做三种产出之一：

- 直接产出一版 `styles.css` 的具体改动方案
- 先补一版更清晰的 HTML 结构建议
- 直接开始改现有页面样式，先做一版可见效果

当前最推荐的下一步，是先基于这份文档直接改一版样式稿，让你能看到实际变化，而不只停留在建议层。
