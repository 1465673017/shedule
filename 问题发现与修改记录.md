# 问题发现与修改记录

> 本文件仅记录已经确认的问题、已经完成的修改，以及确认后尚待修改的问题。
> 未经复现、核实或明确确认的内容不写入本文件。

## 已确认问题

| 编号 | 问题描述 | 当前版本确认依据 | 影响范围 | 确认日期 |
| --- | --- | --- | --- | --- |
| P-001 | 多日期课程的年级系数仍可能按跨日期聚合结果计算，导致柱形图课时费与顶部工资卡不一致。 | `js/app-stats.js` 中顶部工资卡的范围统计仍调用 `calculateSalaryStatsForRange`，该函数继续按日期范围调用 `aggregateLessons`；`aggregateLessons` 会跨日期合并课程后取学生中的最高年级系数，而工资图表使用逐日统计。 | 日、周、月、年课时费图表；工资卡及预计含税工资合计 | 2026-09-10 |
| P-002 | 工资图表与顶部工资卡的显示指标名称和计算口径仍不完全相同。 | `renderSalaryTrendChart` 的数据集标签仍为“课时费”，顶部工资卡同时显示“课时费”和“预计含税工资”；`grossPay` 为底薪加课时费，因此底薪大于 `0` 时两者必然不同。 | 工资统计页面的图表、顶部工资卡及金额比较 | 2026-09-10 |
| P-013 | 周统计的月内周次标签格式仍不统一。 | `js/app-stats.js` 仍保留 `weekNames = ['第一周', '第二周', '第三周', '第四周', '第五周']`，月内周仍会生成中文数字格式；其他周次场景使用日期范围或阿拉伯数字格式。 | 周统计图表横轴及周次识别 | 2026-09-10 |
| P-016 | 同步时在检查结果中只确认部分课程后，未被确认的旧课程没有被清理，导致同步完成后仍保留部分原课程。 | `applyCourseCheckSelections` 只遍历选择值为 `network` 的项目；只有选中的项目会进入导入或 `deleteSingleCellOccurrence` 清理流程，未选中的本地课程不会被处理。用户反馈选择确认部分课程同步后仍有课程未清空。 | 课表检查、部分课程同步、旧课程清理 | 2026-09-11 |

## 已修改问题

| 编号 | 问题描述 | 修改内容 | 修改文件 | 验证结果 | 修改日期 |
| --- | --- | --- | --- | --- | --- |
| P-001 | 多日期课程的年级系数可能按跨日期聚合结果计算，导致工资卡与图表不一致。 | 统计聚合前建立课表投影，并按逐日课程实例计算；工资卡范围统计复用工资图表的逐日课时费结果。 | `js/app-stats.js` | `npm test` 全部通过；工资统计路径已统一按日期读取投影和计算年级系数。 | 2026-09-11 |
| P-013 | 周统计的月内周次标签使用中文数字，和其他周次显示格式不统一。 | 移除中文数字周次数组，统一生成“第1周、第2周、第3周……”格式。 | `js/app-stats.js` | `npm test` 全部通过；月内周标签生成逻辑已统一。 | 2026-09-11 |
| P-016 | 同步时选择确认后，所选范围内的旧课程没有可靠清理。 | 确认覆盖后，按所选日期范围逐日遍历全部课位，调用 `deleteSingleCellOccurrence` 清理现有课程、继承投影和历史遗留投影，再导入网络课程；取消确认时保留本地课程。 | `js/app-course-import.js` | `npm test` 全部通过；确认覆盖路径会先清空所选日期范围内的全部课程，再执行网络导入，并保留异常回滚。 | 2026-09-11 |
| P-017 | 调整实际上课时间时，开始和结束时间的可选范围不符合标准课时的一半规则。 | 原开始时间范围限制在标准课时内，结束时间固定向后扩展 120 分钟，不能按课程总时长动态设置。 | `js/app-attendance.js`、`js/app-courses.js` | 开始时间范围改为“标准开始时间提前半个标准课时至课程中点”；结束时间范围改为“课程中点至标准结束时间延后半个标准课时”；初始值仍为标准课时。以 `15:10-17:10` 为例，开始范围 `14:10-16:10`，结束范围 `16:10-18:10`。开始和保存校验均已同步限制，测试通过。 | 2026-09-11 |
| P-018 | 实际上课时间提示文字已显示新的可选范围，但滑条自身仍使用旧的 `min/max`。 | 修正考勤弹窗中两个 `<input type="range">` 的实际 `min/max` 属性，使其与提示文字和保存校验一致。 | `js/app-attendance.js` | 开始滑条实际范围为“标准开始时间前半个标准课时至课程中点”，结束滑条实际范围为“课程中点至标准结束时间后半个标准课时”；`node --check js/app-attendance.js` 和 `npm test` 均通过。 | 2026-09-11 |
| P-003 | 课程状态使用多个独立颜色点，无法直观表达出勤、请假、缺勤人数比例。 | 参考副本，将课程状态点改为固定尺寸的 `conic-gradient` 比例圆，并用悬停提示显示三类人数。 | `js/app-stats.js`、`styles.css` | 已实现绿色、黄色、红色分段比例圆；`npm test` 全部通过。 | 2026-09-11 |
| P-004 | 人数统计按课程累加显示人次，缺少统计周期内去重实际人数和学生级课程明细。 | 参考副本增加学生 ID 去重统计、实际人数显示、逐日课程明细、出勤状态和实际上课时长，并隐藏人数统计不适用的图表。 | `index.html`、`js/app-stats.js`、`styles.css` | 已实现 `renderStudentStatsDetails` 和 `studentActualCount`；学生明细支持展开收起；`npm test` 全部通过。 | 2026-09-11 |
| P-005 | 学生课程明细缺少学生整体出勤状态比例。 | 在每名学生名称旁增加按出勤、请假、缺勤课程次数生成的比例圆，并保留悬停说明。 | `js/app-stats.js`、`styles.css` | 已实现学生整体状态比例圆及对应样式；`npm test` 全部通过。 | 2026-09-11 |
| P-006 | 学生课程明细日期未显示星期。 | 将学生课程日期格式化为“月.日 周几”，例如 `9.1 周二`。 | `js/app-stats.js` | 已实现日期和星期显示；`npm test` 全部通过。 | 2026-09-11 |
| P-019 | 非标准课时提示标签在课表重绘、切换周次或窗口失焦后可能无法自动收回，残留在页面上遮挡课表。 | 提示框挂载到 `document.body`，原先主要依赖原课程格子的 `mouseleave` 事件移除；课表重绘会销毁原课程格子，可能导致提示框失去收回路径。 | 课表中的非标准课时提示框 | 在课表重绘前增加统一提示框清理，并在窗口失焦和页面隐藏时清理所有 `.non-standard-time-tooltip`。 | `js/app-timetable.js`、`js/app-core.js` | `node --check` 和 `npm test` 均通过。 | 2026-09-11 |

## 待修改问题

| 编号 | 问题描述 | 复现步骤/依据 | 影响范围 | 计划修改内容 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| P-002 | 工资图表与顶部工资卡的显示指标名称和计算口径仍不完全相同。 | `renderSalaryTrendChart` 数据集标签为“课时费”；顶部卡片另有“预计含税工资”，其计算为底薪加课时费。设置底薪大于 `0` 后，直接比较图表与预计含税工资会产生固定差异。 | 工资统计页面的图表、顶部工资卡及金额比较 | 明确图表展示的是“课时费”还是“预计含税工资”，统一标签、悬停提示和比较口径，并补充底薪大于 `0` 的测试。 | 中 |

## 记录格式

### 问题

- **问题描述**：
- **发现依据 / 复现步骤**：
- **影响范围**：
- **关联文件**：
- **确认日期**：

### 修改

- **修改内容**：
- **修改文件**：
- **验证结果**：
- **修改日期**：
## P-020 非标准课时归属判定

- **问题**：非标准课时原先仅按实际上课开始时间落入的标准课时归属，`18:40-20:40` 可能被错误归入 `17:30-19:30`。
- **修改**：改为计算整节实际课时与每个标准课时的重叠分钟数，归入重叠最多的标准课时；重叠相同则按开始时间和课时索引稳定排序。
- **修改文件**：`js/app-course-import.js`、`tests/security-regressions.test.js`
- **验证**：已增加 `18:40-20:40` 在 `17:30-19:30` 与 `19:40-21:40` 之间按重叠时长归入第二节的回归测试。
- **修改日期**：2026-09-11

## P-021 月、年课时统计明细未按逐日课程展示

- **问题**：月统计和年统计的“课时明细”使用跨日期聚合结果，同一课程在统计周期内可能被合并为一行，无法逐日查看课程日期、出勤和实际上课时长。
- **修改**：参考副本，月统计和年统计的顶部汇总继续使用周期聚合数据；下方课时明细改用统计范围内的逐日课程实例，按实际日期分别展示。
- **修改文件**：`js/app-stats.js`
- **验证**：`npm test` 全部通过，相关脚本语法检查通过。
- **修改日期**：2026-09-11
