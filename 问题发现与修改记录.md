# 问题发现与修改记录

## 已确认问题

| 编号 | 问题描述 | 复现步骤/依据 | 影响范围 | 确认日期 |
| --- | --- | --- | --- | --- |
| P-001 | 多日期课程的年级系数被错误地按跨日期汇总结果计算，导致柱形图课时费与顶部工资卡不一致。 | 使用 `C:\Users\Admini\AppData\Roaming\A大橙子课时统计` 中的数据，在 2026 年 8 月统计中复现：图表课时费合计为 `¥22103.20`，顶部“预计含税工资”为 `¥22422.40`，相差 `¥319.20`；悬停 `2026/8/18` 时，柱形图为 `¥691.20`，顶部卡片为 `¥734.40`。代码中 `aggregateLessons` 按课位和班型跨日期合并课程，并把不同日期的学生合并后由 `calculateSalaryStats` 取最高年级系数；图表 `calculateSalaryChartSeries` 则按天计算，形成两套不同口径。具体例：课位 `1-3` 在 `2026-08-03`、`2026-08-10`、`2026-08-17`、`2026-08-24`、`2026-08-31` 上课，只有 `2026-08-10` 的学生为高二；图表只对该日期使用 `1.3` 年级系数，工资卡跨日期合并后将 `1.3` 应用于该课位全部日期。 | 日、月、年课时费图表；悬停日期后的工资卡；预计含税工资合计 | 2026-09-10 |
| P-002 | 图表与顶部卡片的显示指标名称和计算口径不完全相同：图表显示“课时费合计”，卡片显示“预计含税工资”（底薪加课时费）。当前用户数据底薪为 `0`，所以本次主要差异来自 P-001；当设置底薪大于 `0` 时，两者即使课时费计算一致也会天然不同。 | `renderSalaryTrendChart` 的柱形图数据为 `coursePay`，`renderStatsCards` 的“预计含税工资”为 `grossPay = basePay + coursePay`。 | 工资统计页面的图表、顶部工资卡及用户对两者数值的直接比较 | 2026-09-10 |
| P-013 | 周统计的月内周次标签格式不统一，月内周使用“第一周、第二周”等中文数字，其他周次场景使用阿拉伯数字或日期范围。 | 周统计图表横轴可显示“第一周、第二周、第三周”等中文数字格式。 | 周统计图表横轴及周次识别 | 2026-09-10 |

## 已修改问题

| 编号 | 问题描述 | 修改内容 | 修改文件 | 验证结果 | 修改日期 |
| --- | --- | --- | --- | --- | --- |
| P-001 | 多日期课程跨日期聚合导致顶部卡片课时费与柱形图不一致。 | 顶部工资卡的课时费直接引用柱形图按日统计的课时费合计；预计含税工资统一按“底薪 + 柱形图课时费”计算。 | `js/app-stats.js` | 使用用户数据复核：2026 年 8 月课时费与柱形图一致为 `¥22103.20`，预计含税工资在底薪为 `0` 时为 `¥22103.20`。 | 2026-09-10 |
| P-002 | 工资图表悬停时顶部卡片与柱形图的金额口径不一致。 | 日、周悬停及默认状态使用柱形图课时费；月悬停使用柱形图课时费加一次底薪；年悬停使用时间范围内各月份预计含税工资之和。所有场景卡片标签统一为“预计含税工资”。 | `js/app-stats.js` | 日、周默认及悬停状态不加底薪；月悬停加一次底薪；年悬停按范围内每个月分别计算“底薪 + 当月柱形图课时费”后求和；月、年默认未悬停逻辑保持不变。 | 已通过日、周、月、年四种粒度及默认/悬停代码路径检查，标签和金额口径符合要求。 | 2026-09-10 |
| P-013 | 周统计的月内周次标签使用中文数字，和其他周次显示格式不统一。 | 月内周统计横轴原显示为“第一周、第二周”等格式。 | `js/app-stats.js` | 移除中文数字周次数组，统一按阿拉伯数字生成“第1周、第2周、第3周……”格式；自然周日期范围显示不变。 | 已确认月内周统计横轴生成结果统一为阿拉伯数字格式。 | 2026-09-10 |
| P-003 | 课程明细中的出勤、请假、缺勤状态分别显示为多个独立颜色点，无法直观表达三种状态的人数比例。 | 将课程明细中的状态点改为一个固定尺寸的圆形比例图，按出勤、请假、缺勤人数在同一个圆中使用绿色、黄色、红色分段显示，并保留悬停人数说明。 | `js/app-stats.js`、`styles.css` | 已确认课程明细使用 `getAttendanceSuffix` 生成状态标记；代码已改为按三类人数生成 `conic-gradient` 单圆状态图。 | 2026-09-10 |
| P-004 | 人数统计总览按课程累加显示人次，无法直接查看统计周期内去重后的实际学生人数及学生级课程明细。 | 移除人数统计中的第三、第四幅图；折线图下增加统计周期内去重后的实际人数；下方按学生展示逐日课程、出勤状态和实际上课时长，点击学生可展开或收起明细。 | `index.html`、`js/app-stats.js`、`styles.css` | 已按日读取课程实例并按学生 ID 去重，复用每日考勤和学生实际上课时长数据生成学生明细；日、周、月、年范围均按当前选择范围统计。 | 2026-09-10 |
| P-005 | 学生课程明细的学生列表缺少该学生整体出勤状态比例，查看单个学生需要展开全部课程后才能判断状态分布。 | 在每个学生名称后增加固定尺寸的比例圆，按该学生在当前统计周期内的出勤、请假、缺勤课程次数分段显示，并通过悬停查看三类次数。 | `js/app-stats.js`、`styles.css` | 复用课程明细的绿色、黄色、红色比例圆样式，为每名学生根据课程记录生成 `conic-gradient` 状态圆。 | 2026-09-10 |
| P-006 | 学生课程明细中的日期未显示星期，无法快速判断课程对应的星期几。 | 将日期显示为“月.日 周几”格式，例如 `9.1 周二`。 | `js/app-stats.js` | 已根据课程日期计算本地星期，并在每条学生课程明细的日期后显示对应星期。 | 2026-09-10 |
| P-007 | 学生课程明细渲染时第一个学生被代码默认展开，导致进入明细后无需点击就显示第一名学生的课程记录。 | `renderStudentStatsDetails` 为第一个学生添加 `is-open`，并将其按钮的 `aria-expanded` 设置为 `true`。 | `js/app-stats.js` | 已移除第一个学生的默认展开标记，所有学生初始收起，点击对应学生后再展开。 | 2026-09-10 |
| P-008 | Windows 和 macOS 发布版本仍为 `1.3.0`，macOS 截图脚本的输出仅按主题和界面名称组织，且统计视图未完整区分。 | `package.json`、`package-lock.json`、页面版本文案和数据架构版本为 `1.3.0`；截图脚本未将所有界面按功能分类输出。 | Windows/macOS 构建版本标识；macOS UI 截图归档和统计视图覆盖 | 已将应用版本统一为 `1.3.1`；截图脚本补齐基础设置、人数统计和课时费统计，并按主题/功能分类输出 PNG 和 `manifest.json`。 | 2026-09-10 |
| P-009 | 月、年文字统计的课程明细按课位和班型合并展示，无法按日期查看每天具体上课课程。 | 月、年文字统计调用 `aggregateLessons` 生成明细，同一课位在多个日期上课时会被合并为一条记录。 | 月、年课时统计的课程明细查看 | 月、年课程明细改为按日期读取每日课程实例，按日期顺序展示；顶部汇总卡片仍使用原汇总数据。 | 2026-09-10 |
| P-010 | JSON 导入的非标准课时按时间重叠拆分到多个标准课位，无法作为一节完整的非标准课程管理。 | 例如标准课时为 `17:30-19:30`，JSON 导入 `18:40-20:40` 时，原逻辑会按重叠关系拆分到多个课位。 | JSON 导入、课表显示、实际课时统计和工资计算 | 已改为按导入课程开始时间所在的标准课位单独导入，保存完整的实际开始/结束时间和实际分钟数，并标记为非标准课时。 | 2026-09-10 |
| P-011 | JSON 导入非标准课时时，学生实际时长仍按标准课位结束时间截断，例如 `18:40-20:40` 被计算为 `50 分钟`。 | `studentActualMinutesForSlot` 原先将导入实际时间限制在标准课位范围内；同时，旧版本已生成的跨两个课位课程不会被新导入逻辑自动清理。 | JSON 导入后的学生实际时长和历史重复课程 | 已改为学生实际时长使用完整导入区间；历史重复课程需在导入时选择覆盖旧数据，或手动删除旧课位记录后重新导入。 | 2026-09-10 |
| P-012 | 确认覆盖 JSON 导入后，旧版本拆分到后续课位的遗留课程仍可能保留。 | 旧逻辑曾将 `18:40-20:40` 拆到起始课位和后续课位；覆盖清理主要按现有课程实例周期分支处理，未可靠清理所有同日课位投影。 | JSON 覆盖导入后的重复课程和课表显示 | 已增加按导入日期范围遍历全部课位并逐次删除课程的清理逻辑，同时保留范围外的循环课程。 | 2026-09-10 |

## 待修改问题

| 编号 | 问题描述 | 复现步骤/依据 | 影响范围 | 计划修改内容 | 优先级 |
| --- | --- | --- | --- | --- | --- |
