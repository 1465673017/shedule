# 添加课程与添加学生弹窗 UI 改造实施细则

## 1. 改造目标

- 让“添加课程”和“添加学生”与当前课表的圆角卡片、浅色边框、主题色按钮保持一致。
- 统一弹窗的标题区、内容区和操作区，减少旧式居中标题与渐变装饰线造成的视觉冲突。
- 不改变现有保存、编辑、删除、1v1、试听和选课业务逻辑。
- 保持桌面端高效操作，并保证窄屏下按钮和选择器可正常使用。

## 2. 统一结构

两个弹窗统一采用以下三段式结构：

1. `modal-header`：左侧标题与说明，右侧关闭按钮。
2. `modal-body`：字段按 `modal-section` 分组，每组带标题，必要时带辅助说明。
3. `form-actions`：底部右对齐“取消 + 主操作”，编辑状态下删除按钮置于左侧。

弹窗添加专用类，避免继续扩大全局选择器影响：

- 添加学生/科目共用：`entity-editor-modal`。
- 添加课程：`lesson-editor-modal`。
- 标题文字容器：`modal-title-block`。
- 标题说明：`modal-subtitle`。
- 内容区：`modal-body`。
- 字段分组：`modal-section`。

## 3. 添加学生弹窗

- 标题根据状态显示“添加学生”或“编辑学生”；科目模式仍显示对应科目标题。
- 姓名输入作为首要字段。
- 1v1 与试听保留现有按钮和 JS 状态，通过独立的“学生类型”区域呈现，不再挤在姓名标签右侧。
- 年级选择器放在独立分组中。
- 学生模式隐藏老师与颜色字段的既有逻辑保持不变。
- 科目模式仍复用同一表单，但类型区隐藏，老师与颜色区正常显示。

## 4. 添加课程弹窗

- 宽度调整为 600px，便于显示科目和学生列表。
- 科目选择使用紧凑色块，选中时展示主题色边框和选中标记。
- 学生选择区保留既有数据渲染和最多人数限制，增加清晰的计数标签。
- 学生列表设置独立最大高度与滚动条，弹窗本身尽量不因学生数量增长而过长。
- 空状态继续使用现有文案，不改变业务行为。

## 5. 视觉规范

- 弹窗圆角：16px。
- 标题：20px、600 字重、左对齐。
- 说明文字：13px，使用次级文字色。
- 分组背景：主题浅色背景；边框使用 `var(--border-color)`。
- 输入框高度不低于 42px，圆角 10px。
- 内容间距：分组间 14px，分组内 10–12px。
- 操作区顶部使用细分隔线；主按钮在最右侧。
- 新弹窗标题禁用 `.modal-content h3::after` 的旧渐变装饰线。

## 6. 响应式与主题

- 屏幕宽度小于 640px 时，弹窗宽度为 `calc(100vw - 24px)`，内边距收紧。
- 操作按钮在窄屏下等宽排列；空间不足时允许换行。
- 科目与学生卡片允许自动换行。
- 深色主题下分组背景、边框、输入框和说明文字使用现有深色变量，不出现纯白色块。

## 7. 兼容性约束

- 保留所有现有 DOM ID，避免破坏事件绑定与数据读写。
- 保留 `subjectForm`、`addLessonForm` 的提交机制。
- 保留 `oneV1Btn`、`auditionBtn`、`gradeSelector`、`lessonSubjectPicker`、`lessonStudentPicker`。
- 不重命名现有关闭、取消、保存和删除按钮 ID。
- 本次只调整 HTML 结构与 CSS 表现；除非适配文案所需，不修改业务流程。

## 8. 验收标准

- 添加学生、编辑学生、添加科目、编辑科目均能正常打开、保存、取消与关闭。
- 1v1、试听状态切换和年级选择正常。
- 添加课程、编辑课程的科目与学生选择正常，人数限制不受影响。
- 两个弹窗标题、关闭按钮、内容分组和底部操作区视觉一致。
- 亮色与深色主题均无明显突兀色块。
- 640px 以下窗口中无横向溢出，主要按钮可点击。
- HTML/JavaScript 基础检查与 `git diff --check` 通过。

## 9. 同类弹窗审计补充

完成首轮改造后，对其他弹窗按相同规范进行统一：

- 批量导入学生：改为标准标题栏、分组表单和统一操作栏。
- 修改课时：移除下拉框内联视觉样式，使用标准时间选择行。
- 年级编辑：使用与科目编辑一致的颜色和底部操作布局。
- 学生点名：补充标题说明并统一内容区留白。
- 设置、重置、导出：统一标题块、说明文字、关闭按钮和标题装饰。
- 使用教程：移除旧式内联标题栏与关闭按钮样式，统一宽屏内容弹窗外壳。
- 统计弹窗：保留已有专用宽屏结构，只统一通用标题装饰规则，避免破坏图表布局。
